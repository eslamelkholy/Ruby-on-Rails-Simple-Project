
<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
    <div class="form-group">
    
    <%= form.text_field :commenter, :value => current_user.name,:disabled => true,class: 'form-control col-5' %>
  </div>
    
    <div class="form-group">
    <%= label_tag :commenter, 'Comment Content' %>
    <%= form.text_area :body, class: 'form-control col-5' %>
    </div>
    <%= form.submit('Add Comment', :class => "btn btn-success")%>
  </p>
<% end %>